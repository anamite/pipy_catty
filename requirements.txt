pipecat-ai[speechmatics,openai,silero,local,kokoro]
python-dotenv
soxr
httpx

# openwakeword is installed separately by install.sh: it hard-requires
# tflite-runtime on Linux, which has no wheel for many Pi Python builds, even
# though we only use its ONNX backend. See install.sh for the workaround.
